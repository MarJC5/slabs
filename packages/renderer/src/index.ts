/**
 * @slabs/renderer
 *
 * Lightweight browser runtime for rendering blocks on public pages
 */

export { SlabsRenderer } from './SlabsRenderer';

export type {
  RendererOptions,
  RenderContext,
  EditorJSData,
  EditorJSBlock,
  RenderFunction
} from './types';
